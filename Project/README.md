# Info6205FinalProject

Implement MSD radix sort for a natural language which uses Unicode characters.
(UnicodeSorting > src > main > java > edu > neu > coe > info6205 > sort > Unicode)

We have sorted some Hindi language words.

Completed a literature survey of two papers on  Quick Sort and Enhancement of Radix sort (using clustering). And compared MSD with Timsort, Dual-pivot 
Quicksort, Huskysort, and LSD radix sort. 
